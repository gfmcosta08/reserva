/** Limite de linhas na tabela da página /materials (lista principal). */
export const MATERIALS_LIST_ROW_LIMIT = 4000

/** Limite para leituras só de metadados (nomes / localizações / categorias nos filtros). */
export const MATERIALS_FILTER_META_ROW_LIMIT = 10000
