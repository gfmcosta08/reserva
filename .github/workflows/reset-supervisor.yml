name: Reset Supervisor Password
on: workflow_dispatch

jobs:
  reset:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - name: Install deps
        run: npm ci
      - name: Reset supervisor password
        env:
          SUPABASE_TEST_URL: ${{ secrets.SUPABASE_TEST_URL }}
          SUPABASE_TEST_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_TEST_SERVICE_ROLE_KEY }}
          QA_SUPERVISOR_PASSWORD: "ReservaSupervisor2026!"
        run: node scripts/qa/create-supervisor-test.mjs
      - name: Confirm password
        run: |
          echo "Nova senha: ReservaSupervisor2026!"
